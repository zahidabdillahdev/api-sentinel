{
  "name": "@api-sentinel/cli",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "bin": {
    "api-sentinel": "dist/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "eslint src test",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22.10.5",
    "typescript": "^5.7.2",
    "vitest": "^4.1.10"
  }
}
